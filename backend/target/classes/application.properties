spring.application.name=besafe-backend
server.port=8080

# Ruta al archivo JSON de datos (relativa al directorio de ejecución)
app.data.file=data.json

# CORS
cors.allowed-origins=http://localhost:5173
